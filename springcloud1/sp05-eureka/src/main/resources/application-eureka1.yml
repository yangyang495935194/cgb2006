# application-eureka1.yml
# 激活当前profile: --spring.profiles.active=eureka1

eureka:
  instance:
    hostname: eureka1
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka2:2002/eureka